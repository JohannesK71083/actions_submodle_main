name: "Submodule Test"
description: ""

runs:
  using: "composite"
  steps:
    - name: execute test
      shell: python
      run: |
        from os import path, environ
        path = rf"{environ['ACTION_PATH']}\submodule\test.txt"
        print(path)
        print(path.exists(path))
      env:
        ACTION_PATH: $(( github.action_path ))
